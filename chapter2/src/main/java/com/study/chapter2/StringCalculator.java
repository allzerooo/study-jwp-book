package com.study.chapter2;

import java.util.regex.Pattern;

public class StringCalculator {

    public int add(String text) {
        // 요구사항 1. 빈 문자열 또는 NULL 값을 입력할 경우 0을 반환해야 한다
        if (text == null || text.isEmpty()) {
            return 0;
        }
        return 0;
    }
}
